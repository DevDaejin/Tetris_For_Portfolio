using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Title : MonoBehaviour
{
    readonly string[] tetris = { "T", "E", "T", "R", "I", "S" };
    private void Start()
    {
        GameObject go;
        for (int i = 0; i < tetris.Length; i++)
        {
            //go = new GameObject()
            //tetris[i]
        }
    }
}
